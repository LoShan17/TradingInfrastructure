from setuptools import setup
from setuptools import find_packages

#['strategies_backtester']

setup(name = 'TradingInfrastructure',
      version = '0.1.1',
      description = 'Package that creates a Trading Infrastructure framework',
      author = 'Daniel Shamoa',
      packages = find_packages(),
      zip_safe = False)
